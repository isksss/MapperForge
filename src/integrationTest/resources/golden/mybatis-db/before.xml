<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="io.github.isksss.mapperforge.integration.MybatisDatabaseIntegrationTest$UserMapper"><resultMap id="userResultMap" type="io.github.isksss.mapperforge.integration.MybatisDatabaseIntegrationTest$UserRow"><id property="id" column="id"/><result property="name" column="name"/><result property="email" column="email"/></resultMap><insert id="insertUser">insert into users (id, name, email, deleted) values (#{id}, #{name}, #{email}, false)</insert><select id="findUsers" resultMap="userResultMap">select id, name, email from users<where>deleted = false<if test="ids!=null&amp;&amp;ids.size()>0">and id in<foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach></if><if test="name!=null">and name = #{name}</if></where>order by id</select></mapper>
