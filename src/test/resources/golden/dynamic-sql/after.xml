<mapper namespace="sample.UserMapper">
    <select id="find" resultType="User">
        SELECT
            id,
            name
        FROM users
        WHERE id = #{id}
        <if test="name != null &amp;&amp; name != ''">
            AND name = #{name}
        </if>
    </select>
</mapper>
