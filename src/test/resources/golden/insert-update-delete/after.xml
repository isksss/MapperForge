<mapper namespace="sample.UserMapper">
    <insert id="insert">
        INSERT INTO users (id,
            name,
            email)
        VALUES (#{id},
            #{name},
            #{email})
    </insert>
    <update id="update">
        UPDATE users
        SET name = #{name},
            email = #{email}
        WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE FROM users
        WHERE id = #{id}
    </delete>
</mapper>
