<mapper namespace="sample.UserMapper"><select id="search" resultType="User"><where><if test="not (name==null||admin)">and name = #{name}</if><if test="enabled?name!=null:admin">and enabled = true</if></where></select></mapper>
